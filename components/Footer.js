import Link from "next/link";
import styles from '../styles/Footer.module.scss';

export default function Footer() {
  return (
    <footer className={styles.footer}>
      div.wrapper>(div.soMe + div.payment + div.schedule)
    </footer>
  )
}
